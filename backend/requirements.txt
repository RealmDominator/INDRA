# INDRA Backend Dependencies
# Phase: Step 5 — PostgreSQL Implementation
# Do NOT add feature-stage libraries here (XGBoost, NetworkX, LLM SDKs, etc.)
# Those are added when their implementation steps begin.

# --- Web Framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# --- Data Validation ---
pydantic>=2.10,<3.0
pydantic-settings>=2.6,<3.0

# --- Database ---
sqlalchemy>=2.0,<3.0
asyncpg>=0.30,<1.0          # async PostgreSQL driver used by SQLAlchemy
psycopg2-binary>=2.9,<3.0  # sync driver for reproducible SQL initialization/check scripts

# --- Migrations ---
alembic>=1.13,<2.0
rapidfuzz>=3.9,<4.0

# --- Step-6A tests ---
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
httpx>=0.27,<1.0
networkx>=3.3,<4.0
