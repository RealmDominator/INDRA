# ML workspace

The Phase-2 XGBoost candidate is not trained. The Step 8D-B audit found no
defensible independent historical disruption labels, so model, dataset,
evaluation, and generated-artifact directories intentionally remain empty
until the requirements in `docs/07-ai-ml/XGBOOST_EVALUATION.md` are met.
